# 🚀 БЫСТРЫЙ СТАРТ - СКВАД

## Проблема: "npm не находит package.json"

Это означает, что вы находитесь не в той папке или файлы не были скачаны.

---

## ✅ Решение: Скачайте проект правильно

### Способ 1: Из этого чата (если есть кнопка Export/Download)

1. Найдите кнопку **"Export"** или **"Download"** в интерфейсе чата
2. Скачайте ZIP-архив
3. Распакуйте его
4. Откройте командную строку В ЭТОЙ ПАПКЕ

### Способ 2: Создайте файлы вручную

Выполните эти команды по порядку:

```bash
# 1. Создайте папку
mkdir SQUAD
cd SQUAD

# 2. Создайте package.json (скопируйте содержимое ниже)
```

Создайте файл `package.json` с таким содержимым:

```json
{
  "name": "squad-social",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.94.0",
    "clsx": "2.1.1",
    "date-fns": "^4.1.0",
    "lucide-react": "^0.563.0",
    "react": "19.2.3",
    "react-dom": "19.2.3",
    "react-router-dom": "^7.13.0",
    "tailwind-merge": "3.4.0",
    "zustand": "^5.0.11"
  },
  "devDependencies": {
    "@tailwindcss/vite": "4.1.17",
    "@types/node": "^22.0.0",
    "@types/react": "19.2.7",
    "@types/react-dom": "19.2.3",
    "@vitejs/plugin-react": "5.1.1",
    "tailwindcss": "4.1.17",
    "typescript": "5.9.3",
    "vite": "7.2.4"
  }
}
```

---

## 📁 Как проверить что вы в правильной папке

В командной строке выполните:

**Windows:**
```bash
dir
```

**Mac/Linux:**
```bash
ls -la
```

Вы должны увидеть файл `package.json` в списке!

---

## 🔧 Пошаговая инструкция

### 1. Откройте правильную папку

Если вы распаковали архив в `D:\SQAD Opus 4.5\`, то внутри должна быть ещё одна папка с файлами. Попробуйте:

```bash
cd "D:\SQAD Opus 4.5"
dir
```

Если видите там другую папку (например `squad` или `project`), войдите в неё:

```bash
cd squad
dir
```

### 2. Убедитесь что package.json есть

```bash
dir package.json
```

Должно показать файл, а не ошибку.

### 3. Установите зависимости

```bash
npm install
```

### 4. Запустите

```bash
npm run dev
```

---

## ❓ Если ничего не помогает

Создайте проект с нуля:

```bash
# 1. Перейдите в удобную папку
cd C:\Projects

# 2. Создайте папку SQUAD
mkdir SQUAD
cd SQUAD

# 3. Создайте package.json вручную
# (создайте файл через Блокнот и сохраните как package.json)

# 4. После создания package.json
npm install

# 5. Создайте остальные файлы из чата

# 6. Запустите
npm run dev
```

---

## 📞 Контрольный список

Убедитесь что:
- [ ] Вы в папке, где есть файл `package.json`
- [ ] Node.js установлен (`node --version` показывает версию)
- [ ] npm работает (`npm --version` показывает версию)
