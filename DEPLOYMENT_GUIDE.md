# 🚀 Полная инструкция по развертыванию СКВАД

## Содержание
1. [Подготовка файлов](#1-подготовка-файлов)
2. [Настройка Supabase](#2-настройка-supabase)
3. [Развертывание на Vercel](#3-развертывание-на-vercel)
4. [Развертывание на Netlify](#4-развертывание-на-netlify)
5. [Подключение домена](#5-подключение-домена)
6. [Настройка SSL](#6-настройка-ssl)

---

## 1. Подготовка файлов

### 1.1 Скачайте проект

Создайте папку проекта и скопируйте все файлы:

```
squad/
├── src/
│   ├── components/
│   │   ├── Layout.tsx
│   │   ├── CreatePostModal.tsx
│   │   └── PostCard.tsx
│   ├── pages/
│   │   ├── LoginPage.tsx
│   │   ├── RegisterPage.tsx
│   │   ├── FeedPage.tsx
│   │   ├── ProfilePage.tsx
│   │   ├── SearchPage.tsx
│   │   ├── ExplorePage.tsx
│   │   └── MessagesPage.tsx
│   ├── store/
│   │   └── useStore.ts
│   ├── types/
│   │   └── index.ts
│   ├── utils/
│   │   └── cn.ts
│   ├── App.tsx
│   ├── main.tsx
│   └── index.css
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
├── netlify.toml
├── vercel.json
├── .env.example
└── DATABASE_SCHEMA.sql
```

### 1.2 Установите зависимости

```bash
cd squad
npm install
```

### 1.3 Проверьте локально

```bash
npm run dev
```

Откройте http://localhost:5173

---

## 2. Настройка Supabase

### 2.1 Создайте проект

1. Перейдите на https://supabase.com
2. Нажмите "Start your project"
3. Войдите через GitHub
4. Нажмите "New Project"
5. Заполните:
   - **Name**: squad
   - **Database Password**: (сохраните!)
   - **Region**: выберите ближайший (EU West для России)
6. Нажмите "Create new project"
7. Дождитесь создания (2-3 минуты)

### 2.2 Получите ключи API

1. В левом меню нажмите ⚙️ **Settings**
2. Перейдите в **API**
3. Скопируйте:
   - **Project URL** (например: `https://xxxxx.supabase.co`)
   - **anon public** ключ

### 2.3 Создайте таблицы

1. В левом меню нажмите **SQL Editor**
2. Нажмите "New Query"
3. Скопируйте содержимое файла `DATABASE_SCHEMA.sql`
4. Вставьте в редактор
5. Нажмите "Run" (Ctrl+Enter)
6. Убедитесь, что нет ошибок

### 2.4 Настройте аутентификацию

1. В левом меню **Authentication**
2. **Providers** → включите **Email**
3. (Опционально) Включите **Google**:
   - Перейдите в Google Cloud Console
   - Создайте OAuth 2.0 credentials
   - Добавьте redirect URL из Supabase
   - Вставьте Client ID и Secret в Supabase

### 2.5 Создайте Storage Bucket

1. В левом меню **Storage**
2. Нажмите "New bucket"
3. Название: `avatars`
4. ✅ Public bucket
5. Нажмите "Create bucket"

### 2.6 Настройте политики Storage

1. Выберите bucket `avatars`
2. Вкладка **Policies**
3. Нажмите "New Policy" → "For full customization"
4. Создайте политики:

**Политика на чтение (SELECT):**
```sql
CREATE POLICY "Avatars are public"
ON storage.objects FOR SELECT
USING (bucket_id = 'avatars');
```

**Политика на запись (INSERT):**
```sql
CREATE POLICY "Users can upload avatars"
ON storage.objects FOR INSERT
WITH CHECK (
  bucket_id = 'avatars' AND
  auth.uid()::text = (storage.foldername(name))[1]
);
```

---

## 3. Развертывание на Vercel (рекомендуется)

### 3.1 Подготовка

1. Создайте аккаунт на https://vercel.com (через GitHub)
2. Создайте репозиторий на GitHub
3. Загрузите файлы проекта в репозиторий:

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/squad.git
git push -u origin main
```

### 3.2 Деплой

1. На Vercel нажмите "Add New" → "Project"
2. Выберите репозиторий `squad`
3. Framework Preset: **Vite**
4. **Environment Variables** — добавьте:
   - `VITE_SUPABASE_URL` = ваш Project URL
   - `VITE_SUPABASE_ANON_KEY` = ваш anon key
5. Нажмите "Deploy"
6. Дождитесь завершения (2-3 минуты)

### 3.3 Результат

Ваш сайт доступен по адресу:
`https://squad-xxxxx.vercel.app`

---

## 4. Развертывание на Netlify

### 4.1 Подготовка

1. Создайте аккаунт на https://netlify.com
2. Подключите GitHub

### 4.2 Деплой

1. Нажмите "Add new site" → "Import an existing project"
2. Выберите GitHub → ваш репозиторий
3. Build settings:
   - **Build command**: `npm run build`
   - **Publish directory**: `dist`
4. **Environment variables**:
   - `VITE_SUPABASE_URL` = ваш Project URL
   - `VITE_SUPABASE_ANON_KEY` = ваш anon key
5. Нажмите "Deploy site"

---

## 5. Подключение домена

### 5.1 Покупка домена

Купите домен на одном из регистраторов:
- **REG.RU** (Россия): https://reg.ru
- **Namecheap**: https://namecheap.com
- **Cloudflare**: https://cloudflare.com/products/registrar/

### 5.2 Настройка DNS для Vercel

1. В Vercel откройте проект
2. **Settings** → **Domains**
3. Добавьте ваш домен (например: `squad.ru`)
4. Vercel покажет DNS записи, которые нужно добавить

На сайте регистратора домена:
1. Откройте настройки DNS
2. Добавьте записи:

| Тип | Имя | Значение |
|-----|-----|----------|
| A | @ | 76.76.19.19 |
| CNAME | www | cname.vercel-dns.com |

### 5.3 Настройка DNS для Netlify

На сайте регистратора:

| Тип | Имя | Значение |
|-----|-----|----------|
| A | @ | 75.2.60.5 |
| CNAME | www | ваш-сайт.netlify.app |

### 5.4 Ожидание

DNS записи обновляются от 10 минут до 48 часов.

---

## 6. Настройка SSL

### Vercel
SSL включается автоматически после подключения домена.

### Netlify
SSL включается автоматически. Проверьте:
1. **Domain settings** → **HTTPS**
2. Должен быть статус "Certificate issued"

---

## 📝 Дополнительные настройки

### Обновление URL в Supabase

После подключения домена обновите настройки в Supabase:

1. **Authentication** → **URL Configuration**
2. Site URL: `https://squad.ru`
3. Redirect URLs: добавьте `https://squad.ru/*`

### Создание файла конфигурации Supabase

Создайте файл `src/lib/supabase.ts`:

```typescript
import { createClient } from '@supabase/supabase-js'

const supabaseUrl = import.meta.env.VITE_SUPABASE_URL
const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY

export const supabase = createClient(supabaseUrl, supabaseAnonKey)
```

### Установка Supabase клиента

```bash
npm install @supabase/supabase-js
```

---

## 🔧 Решение проблем

### Ошибка 404 при обновлении страницы

Для SPA нужна настройка редиректов. Файлы `vercel.json` и `netlify.toml` уже содержат нужные настройки.

### Ошибка CORS

В Supabase → Settings → API:
- Проверьте, что ваш домен добавлен в "Additional Redirect URLs"

### Аватары не загружаются

1. Проверьте, что bucket `avatars` публичный
2. Проверьте политики Storage
3. Проверьте права пользователя

---

## 📞 Контрольный список

- [ ] Supabase проект создан
- [ ] Таблицы созданы (SQL выполнен)
- [ ] Storage bucket создан
- [ ] API ключи скопированы
- [ ] Проект загружен на GitHub
- [ ] Деплой выполнен (Vercel/Netlify)
- [ ] Домен подключен
- [ ] SSL работает
- [ ] Supabase URL обновлен
- [ ] Тестовая регистрация прошла успешно

---

## 🎉 Готово!

Ваша социальная сеть СКВАД теперь работает на вашем домене!

Для поддержки и вопросов создавайте issues в репозитории.
